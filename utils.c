#include "utils.h"

char** splitStringV2(char *string, int count, char delimiter) {
    char **subStrings = (char**) malloc(count * sizeof(char*));

    for (int i = 0; i < count; i++) {

    }

    return NULL;
}